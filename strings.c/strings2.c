#include <stdio.h>
int main()
{
    //char str[]= {'R','A','J','\0'};
    //char *ptr=str;
    char *ptr="RAJ"; 
    //OR char ptr[]= "RAJ";
    printf("%s",ptr);
    return 0;
}
// like %d,%f,%c etc %s is also used to describe the string data type.