#include <stdio.h>
int area(int length, int breadth);
int perimeter(int length, int breadth);
int main()
{
int a,p,l,b;
printf("Enter length and breadth");
scanf("%d %d, &l,&b");
a= int area(int length, int breadth);
p= int perimeter(int length, breadth);
printf("Area is %d",a);
printf("Perimeter is %d",p);
return 0;
}
int area(int length, int breadth)
{
    a= l*b;
}
int perimeter(int length, int breadth)
{
    p= 2*(l+b);
}
